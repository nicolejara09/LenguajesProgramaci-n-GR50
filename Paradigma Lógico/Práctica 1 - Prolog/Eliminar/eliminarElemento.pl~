remove(_,[],[]).

remove(X,[X|W],Y) :-
    remove(X,W,Y),!.

remove(X,[F|W],[F|W1]) :-
    remove(X,W,W1).

%Y es la lista creada sin el elemento que se eliminó.

%X es el elemento que se buscará para eliminarlo de la lista.

%W es la cola, esta recorrerá la lista hasta hacer matching con el
%elemento que se quiere eliminar.
